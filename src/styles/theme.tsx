import { createMuiTheme } from '@material-ui/core/styles'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#4fd2c2'
    },
    secondary: {
      main: '#3398dc'
    },
    error: {
      main: '#f03434'
    },
    background: {
      default: '#f7f7f7'
    }
  }
})

export default theme
